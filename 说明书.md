# NekoriEmojy 第一可用版候选使用说明

版本：0.1.0-rc1。基于 SuzuEmojy，沿用日常操作习惯；本轮不含 OCR。

## 程序与资源库

完整解压 ZIP，双击根目录 `NekoriEmojy.exe`，保留完整 bin。首次启动在程序目录以外创建空库，或打开已有 Nekori 库。
库根 `nekori-library.json` 标识该库；`data/images` 存图片，`data/*.db` 保存分类、Tag、排序和偏好。同图加入多分类/小分类只存一份，不需要为每张图建文件夹。

更新时完全退出程序，将新程序解压到另一个程序目录，再打开原库。请勿只保留图片而丢弃数据库，或手动改名/移动库内图片。
记忆路径丢失、库移动后，可重新选择包含 `nekori-library.json` 的库根。

## 从 SuzuEmojy 迁入

1. 在旧版确认图库可用，完全退出旧 Suzu，保留旧程序和完整 data。
2. 首次启动 Nekori 可先建空库，再进入设置 →“资源库与诊断”→“从旧 SuzuEmojy 只读迁入”。
3. 选择旧版包含 images 的 data（通常在旧程序 bin/data），再选一个新的空文件夹作为目标。
4. 等待复制核验。DB 与 JSON 冲突时会显示各层数量、报告并逐项选择来源，不自动用空 DB 覆盖 JSON。
5. 检查迁入数量、分类、Tag、排序及图片。报告在新库 `data/reports/legacy-*.json`，原始快照在 `data/snapshots/suzu-*`。旧源始终不修改。

目标保留原始快照和可用图库，请预留至少源 data 两倍以上空间。冲突或失败时，目标旁可能留有 `.legacy-*` 核验目录，不要当作可用库。
先看报告再重试到空目标，不要清空原库。未知/不可对应字段仍保存在快照中，不保证都转成可编辑项目。

## 日常整理

- **导入**：拖入图片/文件夹、复制粘贴或使用原入口。外部文件保持原位；inbox 在库的 `data/inbox`，导入后保留来源。
- **分类**：侧边栏大分类支持拖放归类、重命名、排序，同图可属于多个分类。
- **小分类**：进入大分类后使用“小分类”入口创建并命名，向分组标题拖入图片，点击标题左侧箭头折叠/展开。各组平铺共享资源；删除小分类只改变关系。
- **搜索/筛选**：搜索人工填写的普通 Tag，配合原筛选器；没有 OCR 自动识字。搜索时相关小分类临时展开，清空搜索恢复折叠。
- **多选**：批量归类、移出或删除；同图出现在多个小分类时按一个资源计数。
- **最近与发送**：沿用复制、发送、最近记录和快捷面板。默认主窗口 `Ctrl+Shift+E`，快捷面板 `Alt+2`，可在设置修改。
- **退出**：关闭主窗口只隐藏；完全退出使用托盘菜单，等待后台任务结束。

设置为独立窗口；预览后保存才提交，关闭可放弃未保存预览。已完成的库复制迁移、偏好导入导出及手动维护继续可用。偏好 JSON 不是图库备份。

## 备份、交换与回退

**完整备份/换位置**：用设置“完整复制并迁移当前库”复制到空目录，源库保留；也可完全退出后复制整个资源库。重新打开副本确认无误后，再决定如何保管原库。

**交换图片**：资源包页提供导入、导出全部或指定分类。exchange v1 只带旧格式支持的 PNG/GIF、普通 Tag、大分类关系；WebP/WebM/APNG 等会被跳过并记录。小分类、手工排序、最近记录和设置不在包中。

**回到 Suzu**：退出 Nekori 后继续打开保留的原版与原 data。要带回新增图片，可将 Nekori 资源包导入已经备份的 Suzu 库。固定基线原版能接收 PNG/GIF、Tag 和非空大分类关系，但会忽略空分类，不能恢复 Nekori 小分类/排序。原版导出器的 Tag 拆分问题可能留在旧包中，直接只读迁入可保留完整 Tag。

请勿让 Suzu 直接打开 Nekori 资源库，也不要从两个程序同时编辑同库。Nekori 新功能整理结果的安全退路是完整 Nekori 库备份，不是 v1 包。

## 验证边界

测试采用合成图库，未读取用户真实旧数据。Windows 11 本机验证不能替代干净机器、其他 CPU、跨盘、断电和长期使用验证。候选版尚待发布前 Final Review；遇到问题保留原库和报告，不要先删除数据重建。

[源码与反馈](https://github.com/Nekori347/NekoriEmojy)。上游作者 IxinorTyan；GPL-3.0。
