# 用户增补：2026-09-14

本文是用户后续明确需求，优先于原交接中未明确/不同的对应部分；不改写历史交接原文。

1. 参考上游针对 issues #7（覆盖升级丢分类/排序）、#8（首次导入慢）、#9（缩放光标残留）的修复。先检查固定 SHA 的 diff，记录实际吸收内容；独立资源库方案继续有效，不同步覆盖 Nekori 改动。
2. 用户明确选择：软件内分类，图片只存一份。资源存放在普通图片目录，通过相对路径索引管理；不为每张图片创建独立目录。大分类、小分类均为数据库关联，分类动作不移动/复制物理图片；同图可属于多个大分类。
3. 每个用户大分类内部增加一层用户创建、命名、添加内容的可折叠小分类。表现为内容区平铺的分组标题与展开/收起内容，不引入任意深度子树或多级弹出菜单。纳入 P3。
4. 小分类必须随库保留；名称、成员、显示顺序和折叠状态需持久化。删除小分类只解除该组关联，图片及大分类成员保留。同一大分类内的图片可放入多个小分类；未加入小分类的图片有明确展示位置。搜索仍能找到折叠组内的资源。整库复制/升级保留这些状态；exchange v1 未包含新分组，不能宣称它可备份这些状态。
5. 功能完成后重写 GitHub README / 仓库简介，呈现 NekoriEmojy 的实际功能、独立库使用、发行方式和状态，保留 GPL / 上游作者与来源，不把计划写成已发布能力。

新增验收：创建/重命名/删除组，展开/收起和重连恢复，同图多组/多分类，分组批量添加，删除组后资产不丢失，分类切换与搜索，整库复制及升级恢复。逻辑规则取自当前明确需求及保持既有多分类语义的实现选择；详细 UI 随 P3 审阅。
