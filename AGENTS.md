# NekoriEmojy 开发约束

完整产品契约是 [单文件交接 v1.1](docs/spec/NekoriEmojy-handoff-v1.1.md)。实施前阅读它以及 [阶段记录](docs/development/STATUS.md)。当前用户要求优先；交接文件中“本次仅整理文档、未开发”等句子是历史事实，不撤销当前开发授权。

- 长期仓库为 `Nekori347/NekoriEmojy`；保留 `IxinorTyan/SuzuEmojy` 的历史和 upstream 关系。遵守 [上游维护规则](docs/development/UPSTREAM.md)，不得无条件同步、强推或覆盖 Nekori 修改。
- 按 P0 → P1 → P2 → P3 → P4 → P5 → P6 的依赖推进，逐阶段记录证据、限制和回滚。P4 可按契约提前做隔离评估。
- 保留原有功能、GPL-3.0、作者和来源。复用当前 Python/PySide/Fluent 栈；不整体重写。
- 用户只选择独立资源库根目录。程序目录可覆盖和删除；用户资源、整理状态、偏好、机器/人工 OCR 和有效索引随库保存。
- 正常运行数据库为权威来源；旧 JSON 只在迁移边界读取。不能以缓存修复、重连或升级为由重写人工分类、顺序和 OCR。
- 默认复制导入且源文件不变；多个 UI 入口共享导入及分类核心。任务绑定原库上下文，不随全局活动库改变而写错库。
- 旧源只读；迁移前保护、一致快照、校验后切换、失败可回退。不在唯一真实图库做实验，不删除唯一旧 JSON/DB。
- OCR 先完成实测评估再定后端；人工文本和人工清空受保护，机器结果晚到也不能覆盖；搜索不实时 OCR，GIF 不默认全帧识别。
- 日志有界、低开销，不默认收集剪贴板/图片/私人 OCR，不自动上传；诊断失败不影响主程序。
- 使用标准库和已有依赖优先。新增第三方方案按 [决策清单](docs/development/DECISIONS.md) 检查兼容、维护、许可、成本和替换边界。
- ZIP 不包含运行数据、私人配置和开发缓存。旧 exchange v1 不等于整库备份，不宣称恢复其未保存的手工顺序。
- 自动化集中验证数据与并发契约。Windows 原生交互、实际包体、真实包和真实事故，只对实际测过的范围下结论。
- 工作副本和构建环境由开发工具管理；不要求用户维护本地 Git 工作区。未推送时必须明确说未推送。
